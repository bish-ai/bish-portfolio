import type { NextConfig } from "next";

// next.config.ts has been moved to next.config.mjs for AWS Amplify compatibility.
// Original contents were copied to next.config.ts.bak in this branch.

// This file is intentionally left minimal to avoid AWS Amplify detecting a TypeScript Next config.
const placeholder: Partial<{}> = {};

export default placeholder;
